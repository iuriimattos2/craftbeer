server:
  port: 9000

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:file:~/craftbeer
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:      ddl-auto: update
    show-sql: false